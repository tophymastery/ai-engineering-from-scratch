apiVersion: v1
kind: Service
metadata:
  name: cart
  labels:
    app: cart
    shop.io/team: marketplace
spec:
  selector:
    app: cart
  ports:
    - name: http
      port: 8104
      targetPort: 8104
