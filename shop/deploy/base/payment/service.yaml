apiVersion: v1
kind: Service
metadata:
  name: payment
  labels:
    app: payment
    shop.io/team: payments
spec:
  selector:
    app: payment
  ports:
    - name: http
      port: 8106
      targetPort: 8106
