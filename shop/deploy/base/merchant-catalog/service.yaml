apiVersion: v1
kind: Service
metadata:
  name: merchant-catalog
  labels:
    app: merchant-catalog
    shop.io/team: discovery
spec:
  selector:
    app: merchant-catalog
  ports:
    - name: http
      port: 8102
      targetPort: 8102
